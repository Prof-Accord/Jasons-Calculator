<h2>Jason's Calculator</h2>
<p>
  <%= sprintf('今日は%s年%s月%s日です', @today.year, @today.month, @today.day) %>
</p>
<div>
  <form method="post" action="/result">
  <input required type="text" name="number" maxlength="3" pattern="^\d+$">
  <input type="submit" value="計算する">
</div>
